/*
Q2) tail of doubly linked list is given. need to find local maxima and local minima
using critical point. dont consider head and tail.
local maxima - if both neighbours are smaller than the curr node.
local minima - if both neighbours are larger than the curr node.
*/

